# Tests

This directory contains test files for the Frankodirador project.

## Running Tests

Use pytest to run all tests:

```bash
pytest tests/
```

With coverage reporting:

```bash
pytest tests/ --cov=src --cov-report=html
```
